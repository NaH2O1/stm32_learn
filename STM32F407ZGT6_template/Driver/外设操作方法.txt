-- Driver.c 文件 主要完成初始化外设的初始化
	--  开启时钟
	--  定义一个初始化的结构体
	--  配置一个初始化结构体成员
	--  调用初始化函数，把配置好的结构体成员参数写入寄存器
	--  配置寄存器初始化状态，初始化一个状态


-- Driver.h 文件 主要设定启外设的相应的状态操作
		--  防止重复包含定义全局的宏
		--  使用宏定义优先定义一下要操作外设的信息（引脚，时钟等等再.c文件中使用）
		--  使用带参宏封装相关外设操作
		--  声明Driver.cz中使用的函数

--- CMSIS标准,实际是新建了一个软件抽象层,用户通过CMSIS来操作MCU
	-- 内核函数层：其中包含用于访问内核寄存器的名称、地址定义，主要由ARM公司提供。
	-- 设备外设访问层：提供了片上的核外外设的地址和中断定义，主要由芯片生产商提供。